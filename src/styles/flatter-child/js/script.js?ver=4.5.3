jQuery(document).ready(function($) {
    $( "#accordeon" ).accordion({
        collapsible: true
    });

    $('#btn_newsletter').bind('click', function(e) {
		e.preventDefault();
		$('#pop_newsletter').bPopup({
            opacity: 0.8,
            modalColor: '#484848',
            zIndex: 12000
        });
    });

    $('#btn_prod0').bind('click', function(e) {
	    e.preventDefault();
	    $('.pprod0').bPopup({
            opacity: 0.8,
            modalColor: '#484848',
            zIndex: 12000
        });
    });

    $('#btn_prod1').bind('click', function(e) {
	    e.preventDefault();
	    $('.pprod1').bPopup({
            opacity: 0.8,
            modalColor: '#484848',
            zIndex: 12000
        });
    });

    $('#btn_prod2').bind('click', function(e) {
	    e.preventDefault();
	    $('.pprod2').bPopup({
            opacity: 0.8,
            modalColor: '#484848',
            zIndex: 12000
        });
    });

    $('#btn_prod3').bind('click', function(e) {
	    e.preventDefault();
	    $('.pprod3').bPopup({
            opacity: 0.8,
            modalColor: '#484848',
            zIndex: 12000
        });
    });

    $('#btn_prod4').bind('click', function(e) {
	    e.preventDefault();
	    $('.pprod4').bPopup({
            opacity: 0.8,
            modalColor: '#484848',
            zIndex: 12000
        });
    });

    $('#btn_prod5').bind('click', function(e) {
	    e.preventDefault();
	    $('.pprod5').bPopup({
            opacity: 0.8,
            modalColor: '#484848',
            zIndex: 12000
        });
    });

    $('#btn_prod6').bind('click', function(e) {
	    e.preventDefault();
	    $('.pprod6').bPopup({
            opacity: 0.8,
            modalColor: '#484848',
            zIndex: 12000
        });
    });

    $('#btn_prod7').bind('click', function(e) {
	    e.preventDefault();
	    $('.pprod7').bPopup({
            opacity: 0.8,
            modalColor: '#484848',
            zIndex: 12000
        });
    });

    $('#btn_prod8').bind('click', function(e) {
	    e.preventDefault();
	    $('.pprod8').bPopup({
            opacity: 0.8,
            modalColor: '#484848',
            zIndex: 12000
        });
    });
});